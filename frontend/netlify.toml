[build]
  command = "npm run build"
  publish = "dist"

# SPA catch-all for client-side routing (excludes /api/* since there's no matching file)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
